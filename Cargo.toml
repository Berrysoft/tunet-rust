[workspace]
members = [
    "authtea",
    "netstatus",
    "keyring",
    "tunet-rust",
    "tunet-settings",
    "tunet-model",
    "tunet-model-native",
    "tunet",
    "tunet-gui",
]

exclude = [
    "winrt-bindings",
]

resolver = "2"

[profile.release]
lto = true
codegen-units = 1
