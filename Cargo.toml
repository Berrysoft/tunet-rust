[workspace]
members = [
    "authtea",
    "netstatus",
    "color-theme",
    "keyring",
    "tunet-rust",
    "tunet-settings",
    "tunet-model",
    "tunet-model-native",
    "tunet",
]

exclude = [
    "winrt-bindings",
]

resolver = "2"

[profile.release]
lto = true
codegen-units = 1
