[workspace]
members = [
    "authtea",
    "tunet-rust",
    "tunet",
    "tunet-tui"
]

exclude = [
    "netstatus",
    "keyring",
    "winrt-bindings"
]

resolver = "2"

[profile.release]
lto = true
